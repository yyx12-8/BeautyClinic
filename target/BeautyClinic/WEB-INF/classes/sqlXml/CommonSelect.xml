<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.CommonSelect">
    <select id="findSql" resultType="map">
        ${sql}
    </select>
    <select id="findSqlByCon" resultType="map">
        ${sql}
    </select>
    <select id="findSqlByCon2" resultType="map">
        ${sql}
    </select>
</mapper>