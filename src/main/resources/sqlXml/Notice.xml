<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.Notice">
    <update id="upddateUp">
        update noticelist set message_num_like = #{message_num_like} where message_id = #{message_id}
    </update>
    <update id="upddateCollect">
        update noticelist set message_num_collect = #{message_num_collect} where message_id = #{message_id}
    </update>
    <update id="updatenum">
        update noticelist set message_num_comment = #{message_num_comment} where message_id = #{message_id}
    </update>

</mapper>